| Field Name          | Type         | Role        | Description                                     |
| ------------------- | ------------ | ----------- | ----------------------------------------------- |
| chunk_id            | VARCHAR      | Primary Key | Unique identifier for each text chunk           |
| document_id         | VARCHAR      | -           | Identifier linking chunk to its parent document |
| chunk_order         | INT32        | -           | Sequence order of chunk within document         |
| base_url            | VARCHAR      | -           | Base URL of the source                          |
| canonical_url       | VARCHAR      | -           | Specific page URL                               |
| crawl_date          | INT64        | -           | Timestamp when data was collected               |
| doc_last_modified   | INT64        | -           | Last modified timestamp                         |
| content_type        | VARCHAR      | -           | Type of content (text/image/mixed)              |
| content_source_type | VARCHAR      | -           | Source type (webpage, PDF, etc.)                |
| language            | VARCHAR      | -           | Language of the content                         |
| chunk_text          | VARCHAR      | -           | Actual chunk text                               |
| chunk_text_vector   | FLOAT_VECTOR | -           | Embedding vector (dimension = 384)              |
| doc_version         | VARCHAR      | -           | Version of document                             |
| is_active           | BOOL         | -           | Flag indicating active content                  |


Milvus Index Configuration
1. Vector Index (Embedding Search)

Field: chunk_text_vector

Index Type: HNSW

Metric Type: COSINE

Parameters:

M = 16

efConstruction = 200

Purpose:

Enables fast and accurate semantic similarity search

Suitable for high-dimensional embeddings

Provides strong recall performance

2. Metadata Indexing

Index Type: INVERTED

Fields:

Milvus Index Configuration
1. Vector Index (Embedding Search)

Field: chunk_text_vector

Index Type: HNSW

Metric Type: COSINE

Parameters:

M = 16

efConstruction = 200

Purpose:

Enables fast and accurate semantic similarity search

Suitable for high-dimensional embeddings

Provides strong recall performance

2. Metadata Indexing

Index Type: INVERTED

Fields:

language

is_active

crawl_date

doc_last_modified

Purpose:

Supports filtering and structured queries

Improves retrieval efficiency when applying conditions
